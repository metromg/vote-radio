{
  "name": "mock-backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "nodemon --exec ./node_modules/.bin/ts-node -- ./src/index.ts",
    "build": "rimraf ./dist && tsc -p ./tsconfig.json --outDir ./dist"
  },
  "author": "Michael Günter",
  "dependencies": {
    "chokidar": "^2.0.4"
  },
  "devDependencies": {
    "@types/chokidar": "^1.7.5",
    "@types/node": "^10.9.4",
    "nodemon": "^1.18.4",
    "rimraf": "^2.6.2",
    "ts-node": "^7.0.1",
    "typescript": "^3.0.3"
  }
}
